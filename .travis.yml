language: node_js

node_js:
  - "6.1"

branches:
  only:
    - master

before_script:
  - npm install -g bower

install:
  - npm install

after_success:
  - git push https://$DEPLOY_USER@$DEPLOY_PWD@$DEPLOY_URL master
